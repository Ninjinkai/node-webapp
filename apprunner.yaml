version: 1.0
runtime: nodejs14

build:
  commands:
    build:
      - npm install --production

run:
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
